# word_diff —— Word 文档版本比较 依赖
# 版本比较核心依赖 python-docx
# 注：进阶分析（批注读取 doc.comments、逐字符格式比较）需要 python-docx >= 1.1.0；
#     本版本未新增任何第三方依赖（XML 层 docDefaults 解析使用 python-docx 自带的 lxml）。
python-docx>=1.1.0

# 测试依赖（可选）
# pytest>=7.0
